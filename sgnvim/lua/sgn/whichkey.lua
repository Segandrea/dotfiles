require('which-key').setup({
  plugins = {
    registers = false,
  },
  presets = {
    operators = false,
    motions = false,
    text_objects = false,
  },
  window = {
    border = 'shadow',
  },
})

-- TODO: make a directory for plugin keybindings and move them all there
